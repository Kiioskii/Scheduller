/**
 * Szablon nowego modułu feature:
 *
 * modules/<feature>/
 *   api/          — wywołania HTTP, klienty, query keys
 *   hooks/        — logika React (TanStack Query, stan)
 *   components/   — UI specyficzne dla feature
 *   index.ts      — publiczne API modułu (importuj tylko stąd)
 *
 * Przykład użycia w App:
 *   import { MyWidget } from '@/modules/my-feature';
 */

// export { fetchSomething } from './api/my-feature.api';
// export { useSomething } from './hooks/use-something';
// export { MyWidget } from './components/my-widget';
